<UserControl x:Class="CandyHandyman.Desktop.CertificationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="技能认证管理" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="工匠" Binding="{Binding HandymanProfile.User.Nickname}" Width="120"/>
                <DataGridTextColumn Header="技能" Binding="{Binding SkillName}" Width="120"/>
                <DataGridTextColumn Header="证书名称" Binding="{Binding CertificateName}" Width="160"/>
                <DataGridTextColumn Header="证书编号" Binding="{Binding CertificateNo}" Width="140"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="查看证书" Padding="8,2" Margin="0,0,4,0"/>
                                <Button Content="通过" Padding="8,2" Margin="0,0,4,0" Foreground="Green"/>
                                <Button Content="拒绝" Padding="8,2" Foreground="Red"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>