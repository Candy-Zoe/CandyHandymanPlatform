<UserControl x:Class="CandyHandyman.Desktop.VerificationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="实名认证管理" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="用户" Binding="{Binding User.Nickname}" Width="120"/>
                <DataGridTextColumn Header="真实姓名" Binding="{Binding RealName}" Width="100"/>
                <DataGridTextColumn Header="身份证号" Binding="{Binding IdCardNumber}" Width="160"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="提交时间" Binding="{Binding CreatedAt}" Width="160"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="查看证件" Padding="8,2" Margin="0,0,4,0"/>
                                <Button Content="通过" Padding="8,2" Margin="0,0,4,0" Foreground="Green"/>
                                <Button Content="拒绝" Padding="8,2" Foreground="Red"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>